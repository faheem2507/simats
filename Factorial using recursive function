#include<stdio.h>
int factorial(int n)
{
	if(n<=1)
		return n;
	return n*factorial(n-1);
}
int main()
{
	int n;
	printf("ENTER THE NUMBER:\n");
	scanf("%d",&n);
	printf("FACTORIAL = %d",factorial(n));
	return 0;
}
